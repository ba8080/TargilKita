[Unit]
Description=Gunicorn for MidProject
After=network.target

[Service]
User=ec2-user
WorkingDirectory=/opt/midapp
EnvironmentFile=/opt/midapp/.env
ExecStart=/opt/midapp/venv/bin/gunicorn --bind 0.0.0.0:8000 wsgi:app
Restart=always

[Install]
WantedBy=multi-user.target
